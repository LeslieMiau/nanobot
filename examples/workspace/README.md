Example workspace assets live here instead of the repository root.

Use this directory as the reference layout for:
- custom workspace skills under `skills/`
- generated content under `content_queue/`
- other runtime-only files such as `memory/` and `sessions/`

The package's built-in skills remain under `nanobot/skills/`.
