# Example 3: Re-Intercept On Execution Drift

User request:

> Scan the repo, inspect all related logs, and produce a comprehensive report.

Expected behavior:

- Initial or mid-execution reassessment crosses into `large` or `extreme`.
- Emit the standard interception block.
- Stop and wait for user confirmation instead of continuing.
